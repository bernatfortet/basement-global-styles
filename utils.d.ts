import * as React from 'react'
import * as ReactNative from 'react-native'

import * as types from './types'

declare module '@bernatfortet/global-styles/utils' {
  export const parseUnits: (unit: string | number) => number
}
